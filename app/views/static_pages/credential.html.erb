<h1>Issue Credential</h1>

<p>
	<img src="<%= "data:image/png;base64," + Base64.strict_encode64(
		Barby::QrCode.new(@credential_offer, level: :q, size: 18).to_png({ xdim: 8 })
	).html_safe %>">
</p>
<a href="<%= @credential_offer %>">Open from wallet...</a>
